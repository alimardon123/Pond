[package]
name = "pond_streaming_lens"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "StreamingLens — chunked storage for large objects (Rust port)"

[lib]
name = "pond_streaming_lens"
crate-type = ["rlib", "staticlib", "cdylib"]

[dependencies]
pond_kernel = { path = "../../../core/kernel" }
pond_storage = { path = "../../../core/storage" }
pond_core = { path = "../../../core/codec" }
pond_record = { path = "../../../core/record" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = { workspace = true }
