[package]
name = "pond_python"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pond storage — PyO3 bindings to Rust core (Python module name: pond_rust)"

[lib]
name = "pond_rust"           # import pond_rust from Python
crate-type = ["cdylib"]      # produces pond_rust.so / .pyd / .dylib

[features]
default = ["s3"]
s3 = ["dep:pond_s3"]

[dependencies]
pond_core = { path = "../../../core/codec" }
pond_kernel = { path = "../../../core/kernel" }
pond_storage = { path = "../../../core/storage" }
pond_s3 = { path = "../../../core/s3", optional = true }
pyo3 = { version = "0.22", features = ["extension-module"] }
serde_json = "1"
