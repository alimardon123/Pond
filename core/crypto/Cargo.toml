[package]
name = "pond_crypto"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Per-subject encryption and key destruction — erasure for immutable, content-addressed storage"

[dependencies]
pond_kernel = { path = "../kernel" }
chacha20poly1305 = "0.10"
sha2 = "0.10"
getrandom = "0.3"
hex = "0.4"

[dev-dependencies]
tempfile = { workspace = true }
